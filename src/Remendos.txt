Contava letras:

Talvez seja o problema para contagem de caracter especial.

int Dp::n_letter(const char* l){
	if(!l[0]) return 0;
	return ((l[0]!='\n')?1:0)+n_letter(l+1);
}

int Dp::loop(const char *c){	
	if(!c[0]) return 0;
	else if(c[0]<0) return 1+loop(c+1);
	else return 0+loop(c+1);
}
